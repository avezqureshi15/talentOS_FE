export const TOOLTIP = {
  /** min distance kept between the tooltip box and viewport edges */
  VIEWPORT_MARGIN: 8,
  /** vertical gap between the anchor and the tooltip box */
  ANCHOR_GAP: 8,
  /** min distance the arrow keeps from the tooltip box corners */
  ARROW_MIN_OFFSET: 10,
  MAX_WIDTH: 320,
  DEFAULT_OPEN_DELAY: 80,
  DEFAULT_CLOSE_DELAY: 100,
} as const;
